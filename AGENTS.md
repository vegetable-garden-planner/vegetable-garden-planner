# 저장소 작업 규칙

- 작업 전에 `docs/README.md`를 끝까지 읽고, 그 문서가 안내하는 현재 작업 영역 문서만 추가로 읽는다.
- 문서와 코드가 다르면 실제 코드·마이그레이션·라우트·테스트를 확인하고 같은 작업에서 문서를 현재 상태로 수정한다.
- 데이터베이스 스키마의 유일한 기준은 `backend/database/migrations`이다. SQL 스키마 가져오기나 phpMyAdmin 수동 변경으로 구조를 공유하지 않는다.
- Git 기능 커밋, 정리 커밋과 병합 커밋 메시지는 모두 한글로 작성한다.
- 기능은 `feature/*` 브랜치에서 구현하고 검증한 뒤 `develop`에 병합한다.
- 커밋은 독립적으로 설명할 수 있는 유의미한 작업 단위로 나눈다.
