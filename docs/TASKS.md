# 심어봄 작업 목록

기준일: 2026-08-24

이 문서는 아직 끝나지 않은 작업과 바로 이어서 할 순서를 관리하는 단일 기준입니다. 구현 상태 설명은 [PROJECT_STATUS.md](PROJECT_STATUS.md), 진행 중 변경의 상세 인계는 [HANDOFF.md](HANDOFF.md)를 확인합니다.

## 목록 관리 규칙

- `[ ]` 대기, `[-]` 진행 중, `[!]` 막힘, `[x]` 완료 상태를 사용합니다.
- 작업을 시작한 즉시 항목을 `[-]`로 바꾸고 브랜치 이름을 적습니다.
- 검증까지 끝난 작업은 같은 작업 안에서 즉시 `[x]`로 바꾸고 완료일을 적거나, 이후 인계 가치가 없으면 목록에서 제거합니다.
- 완료된 작업을 `[ ]`, `[-]`, “다음 작업”, “남은 작업” 상태로 남겨 두지 않습니다.
- 작업 상태를 바꾼 코드와 이 문서의 상태 변경은 같은 기능 커밋에 포함합니다.
- 진행 목록은 이 문서에만 두고 `README.md`, `PROJECT_STATUS.md`, `HANDOFF.md`에는 중복 목록 대신 이 문서 링크를 둡니다.
- 완료 기록은 최근의 중요한 항목만 최대 10개 유지합니다. 오래된 완료 이력은 Git 기록과 `HANDOFF.md`의 완료 표로 확인합니다.
- 관련 테스트·정적 검사·빌드를 통과하기 전에는 `[x]`로 표시하지 않습니다.

## 진행 중

## 다음 작업

우선순위 순서입니다. 각 항목은 별도 `feature/*` 브랜치에서 진행합니다. 베란다 화분 재배 계획 확장(공간 등록 → 베란다 환경 → 작물 선택 → 화분 배치 → 준비물·일정 → 홈 현황) 로드맵의 남은 단계입니다.

- [ ] 재배 계획 - 화분 - 작물 관계 재구성 — 화분·베란다 시즌의 `featured_crop_id` 단일 작물 제약을 풀어 한 화분에 여러 작물을 담을 수 있게 함(가장 큰 작업, 관계 방향 재검토 필요)
- [ ] 화분 안 작물 배치 저장 — 화분·작물·수량·화분 내 위치를 저장하는 구조(격자 전용인 `garden_layout_placements`를 그대로 못 씀, `garden` 전용 가드 2곳도 함께 검토)
- [ ] 홈 현재 상태 조회 보강 — 메모 저장소, 시즌 전역 최근 기록 조회, "확인 필요" 판정 로직 추가
- [ ] AI 질문용 재배 정보 조회 API — 화분 배치 완료 후 사용자 1명의 현재 재배 정보(공간·환경·배치·기록·메모·다음 일정)를 한 번에 반환하는 읽기 전용 엔드포인트

## 최근 완료

- [x] 작물 데이터 보강 — `crops`에 최소 권장 화분 깊이(`min_pot_depth_cm`, 선택), 햇빛 요구 정도(`sun_requirement`, 기존 `SunlightExposure`(low/partial/full) 재사용, 선택), 지지대 필요 여부(`needs_support`, 필수 boolean)를 추가했다. 기본 재배 기간은 이미 있는 `plantingPeriod`/`harvestPeriod`로 충분해 새로 만들지 않았다. 값은 `database/data/crops.json` 13종에 직접 채우고, 새 마이그레이션이 그 파일을 다시 읽어 기존에 이미 시드된 행도 백필한다(관리자 화면은 읽기 전용이라 편집 UI로는 처리할 수 없다). 프론트 `crop-catalog` 기준정보 미러(`CROP_REFERENCES`)도 값을 맞춰 갱신했다. 값은 기존 요약·관리 안내 문구를 참고한 추정치이며 인용된 출처 문헌으로 검증한 수치는 아니다 — 베란다 화분 재배 계획 확장의 3번(작물 데이터 보강) 항목 — 2026-08-24
- [x] 베란다 환경 보강 — `growing_spaces`에 주변 가림 정도(`shade_level`: none/some/heavy, 선택) 컬럼을 추가했다. 방향(`orientation`)은 이미 nullable이라 그대로 "모름"으로 쓰고, 체감 햇빛(`sunlight`)은 필수(NOT NULL) 3단계였던 컬럼을 nullable로 바꿔 null이 "모름"을 뜻하게 했다(위도·방향 기반 일조 시간 계산에 쓰는 세분화된 방향 값은 그대로 유지 — 스펙의 4방향으로 좁히면 완성된 기능이 깨져서 두지 않았다). 관리자 회원 상세 화면도 null-safe하게 손봤다 — 베란다 화분 재배 계획 확장의 2번(베란다 환경 저장) 항목 — 2026-08-24
- [x] 화분 깊이(depthCm) 저장 지원 — `growing_spaces.depth_cm`(nullable, 1~1000cm)을 추가해 화분 등록 시 가로·세로뿐 아니라 깊이도 저장한다. 시작 진단 3D 화분 미리보기가 이미 입력받던 깊이 값을 공간 등록 폼으로 그대로 이어받는다. 베란다 화분 재배 계획 확장의 1번(화분 저장/API) 항목 — 2026-08-24
- [x] 관리자 콘솔 보강(회원 탈퇴 처리, 배치 격자 시각화, 회원 메모 노출, 운영 도구 톤 재작업) — 회원 목록·상세 화면의 "비활성화" 버튼을 "탈퇴 처리"로 바꾸고 활성 회원 대상일 때 확인 대화상자를 추가했다. 내부 동작은 기존 `ChangeMemberStatus`(비활성화 유지, 데이터 보존)를 그대로 재사용한다 — [관리자 콘솔 계획](ADMIN_CONSOLE_PLAN.md) 2·4번 참고. 회원 상세 화면에 실제 격자 배치를 시각화하는 미니 그리드를 추가하고(추가 쿼리 없이 이미 로드된 데이터로 렌더링), 공간·시즌·재배 일정·재배 기록에 남긴 메모(notes)를 처음으로 노출했다. `admin.css`를 전체 재작성해 소비자 화면과 다른 운영 도구 톤(모노스페이스 수치, 좁은 모서리, 그림자 대신 테두리)으로 바꾸고 로그인 화면의 스톡 사진을 제거했다. 마이그레이션은 없다 — 2026-08-24
- [x] 프로 요금제 결제 인프라(토스페이먼츠 정기결제) — `/plans`에서 토스페이먼츠 카드 자동결제(빌링)로 프로 요금제(월 4,900원)를 구독·해지한다. 구독하기를 누르면 토스 카드등록 페이지로 리다이렉트되고 `/plans/billing-callback`에서 인증 결과(authKey)를 받아 구독을 완료한다. 처음에는 포트원으로 구현했으나 결제 대행사를 토스페이먼츠로 바꿔달라는 요청에 따라 결제 게이트웨이 계층만 다시 구현했다(DB 스키마·Action·컨트롤러 구조는 그대로 재사용). 매일 스케줄러가 결제 기한이 도래한 구독을 자동 청구하고(`subscriptions:charge-due`), 토스 웹훅으로 결제 상태 변경·빌링키 삭제를 반영한다. 카드 등록 인증 응답의 고객 키가 요청자 본인과 다르면 거부되어 다른 사용자 명의로는 구독할 수 없다. **프로 전용 기능(날씨 반영 일정, 사진 비교, 무제한 공유)은 아직 구현되어 있지 않다** — 이번 작업은 결제·구독·해지·정기청구 파이프라인만 구축했다. 닷홈 운영 반영에는 `TOSS_PAYMENTS_*` 환경변수와 웹훅 URL 등록이 필요하다 — [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md) 참고 — 2026-08-21
- [x] iOS 홈 화면 설치(PWA manifest) — 아이폰은 웹사이트를 홈 화면에 추가해 설치해야만(iOS 16.4+) 웹 푸시를 받을 수 있어서 추가했다. `app/manifest.ts`로 매니페스트를 생성하고 브랜드 로고를 초록 배경에 합성해 아이콘(192·512·마스커블 512·애플 터치 180)을 만들었다. 안드로이드 Chrome은 이미 서비스워커만으로 푸시가 되므로 추가 작업이 필요 없었다 — 2026-08-21
- [x] 웹 푸시 알림 — 계정 메뉴 "알림 받기"로 브라우저 푸시 구독을 등록·해제한다(`push_subscriptions` 테이블, `minishlink/web-push`로 VAPID 서명). 매일 07:00 스케줄러가 메일과 함께 오늘 기한 재배 일정·물주기를 푸시로도 보내고, 만료된 구독은 응답을 보고 자동으로 지운다. 요금제 강제가 서버에 없어 모든 회원이 켤 수 있다. 브라우저 자동화 환경은 네이티브 알림 권한 팝업을 조작할 수 없어 구독 등록의 전체 흐름은 로컬에서 실제로 확인하지 못했다 — 초기 상태 판별(서비스워커 등록·기존 구독 조회)은 실제 브라우저에서 정상 동작을 확인했고, 나머지는 백엔드 145개 테스트와 프론트 139개 단위 테스트로 검증했다. **운영 반영에는 `vendor` 재업로드와 마이그레이션, `VAPID_*` 환경변수가 필요하다** — [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md) 참고 — 2026-08-21
- [x] 닷홈 수동 배포의 백업·업로드·마이그레이션·롤백 전체 리허설 — 더미 스크래치 마이그레이션으로 닷홈과 학교 테스트 서버 양쪽에서 phpMyAdmin DB 백업 → FileZilla 업로드 → `migrate --force` → `migrate:status` → `migrate:rollback --force` → `migrate:status` 전체 절차를 실제로 검증. `migrate:rollback`은 `--force` 없이 비대화형 실행하면 프로덕션 확인 프롬프트에 취소된다는 점을 발견해 [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md)에 롤백 절차를 추가했다. 리허설용 파일은 실행 확인 직후 두 서버에서 모두 삭제했다 — 2026-08-21
- [x] 스케줄러·큐·이메일 알림 — 매일 07:00 `notifications:send-daily-reminders`가 오늘 기한인 재배 일정과 물주기를 모아 회원별로 `DailyReminderMail`을 큐에 넣는다(`QUEUE_CONNECTION=database`). 닷홈에는 cron으로 `schedule:run`과 `queue:work --stop-when-empty`를 함께 등록해야 실제 발송된다 — [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md) 5번 참고. 푸시 알림은 별도 작업으로 남겨 둔다 — 2026-08-20
- [x] 회원 탈퇴(본인) — 비활성화 유지 방식으로 결정. `DELETE /me`로 본인 계정을 `disabled`로 바꾸고 세션·API 토큰을 종료한다. 재배 데이터는 삭제하지 않고 보존하며, 개인정보(이메일·닉네임) 익명화는 처리방침에 파기 조항이 생기면 별도 작업으로 진행한다. [관리자 콘솔 계획](ADMIN_CONSOLE_PLAN.md) 2번 참고 — 2026-08-20
