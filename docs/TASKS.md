# 심어봄 작업 목록

기준일: 2026-08-26

이 문서는 아직 끝나지 않은 작업과 바로 이어서 할 순서를 관리하는 단일 기준입니다. 구현 상태 설명은 [PROJECT_STATUS.md](PROJECT_STATUS.md), 진행 중 변경의 상세 인계는 [HANDOFF.md](HANDOFF.md)를 확인합니다.

## 목록 관리 규칙

- `[ ]` 대기, `[-]` 진행 중, `[!]` 막힘, `[x]` 완료 상태를 사용합니다.
- 작업을 시작한 즉시 항목을 `[-]`로 바꾸고 브랜치 이름을 적습니다.
- 검증까지 끝난 작업은 같은 작업 안에서 즉시 `[x]`로 바꾸고 완료일을 적거나, 이후 인계 가치가 없으면 목록에서 제거합니다.
- 완료된 작업을 `[ ]`, `[-]`, “다음 작업”, “남은 작업” 상태로 남겨 두지 않습니다.
- 작업 상태를 바꾼 코드와 이 문서의 상태 변경은 같은 기능 커밋에 포함합니다.
- 진행 목록은 이 문서에만 두고 `README.md`, `PROJECT_STATUS.md`, `HANDOFF.md`에는 중복 목록 대신 이 문서 링크를 둡니다.
- 완료 기록은 최근의 중요한 항목만 최대 10개 유지합니다. 오래된 완료 이력은 Git 기록과 `HANDOFF.md`의 완료 표로 확인합니다.
- 관련 테스트·정적 검사·빌드를 통과하기 전에는 `[x]`로 표시하지 않습니다.

## 진행 중

## 다음 작업

## 최근 완료

- [x] 화분 배치 결과 요약 화면 추가 — 배치 캔버스 다음으로 우선순위가 높다고 지목된 디자인 시안("화분 배치·재배 계획" 결과 화면)을 새 화면(`/seasons/{id}/placements/summary`, `PlacementSummaryView`)으로 구현했다. 시안의 "배치 이유"(상추는 앞쪽에 배치 등 근거 문구)와 "재배 계획 요약"의 구체적 일수(파종~2일 이내, 성장 15~30일, 수확 30~45일)는 뒷받침할 데이터가 카탈로그에 없어 전부 뺐다(과거 지지대 설치 일정 작업에서도 "crops.json에 심은 후 N일 같은 간격 데이터가 없어 임의 날짜를 지어내지 않았다"는 같은 원칙을 재적용). "필요 준비물 흙 리터"는 화분 부피(가로×세로×깊이cm ÷ 1000)로 계산하는 공식을 이번에 새로 도입했다(`placement-summary.ts`) — 앱에 처음 생기는 계산식이라 순수 함수로 분리하고 단위테스트 8개를 붙였다. "환경 적합도"(햇빛·화분 깊이)는 기존 `crop.sunRequirement`/`minPotDepthCm`와 `space.sunlight`/`depthCm`를 실제로 비교해서 보여준다 — 새 데이터 없이 이미 있는 필드만 썼다. 배치 캔버스의 저장 버튼("이 배치로 계속하기 →"로 이름도 바꿈)을 누르면 이 화면으로 이동하도록 연결했고, 크롬으로 두 시즌(상추·시금치 배치 / 실내 화분에 시금치만 배치해 햇빛 부족 배지가 실제로 뜨는 경우)을 직접 열어 숫자·배지·CTA 링크까지 확인했다 — 2026-08-27
- [x] 랜딩 "심어봄이 하는 일" 장면 모바일 터치 진행·정체 구간 수정 — 사용자가 스크린샷으로 지적한 두 가지. (1) 이 장면(scene-how)은 화면 터치로 다음 장면(scene-result)으로 안 넘어가고 스크롤해야만 넘어갔다 — question-scene과 같은 방식(터치 시 한 화면 높이만큼 부드럽게 스크롤)을 추가했다. (2) 모바일에서 "계산 중 100%"에 도달한 뒤 화분이 지어지는 2단계(grow, 2.8초) 동안 화면이 멈춘 것처럼 보였다 — 데스크톱은 이 구간에서 3D 화분이 실제로 자라는 모습을 보여주지만 모바일은 그 시간 동안 진행 바·숫자가 그대로 멈춰 있었다. 계산 완료 후에는 진행 바/숫자가 grow 값을 이어받아 "심는 중 0→100%"로 계속 움직이도록 해서 정체 구간을 없앴다. 사용자가 실제 폰에서 확인해 정상 동작을 확인했다 — 2026-08-27
- [x] 화분 배치를 드래그·탭 캔버스로 개편 — 팀원이 준비한 디자인 시안 중 우선순위가 가장 높다고 지목된 "작물을 화분에 배치" 화면을, select 3개짜리 평범한 폼 목록에서 화분마다 작물을 시각적으로 채워 넣는 캔버스로 바꿨다(`ContainerPlacementEditor`의 `PlacementForm` 내부만 교체 — 온보딩 리다이렉트와 이후 수동 편집이 이미 같은 컴포넌트를 쓰고 있어 화면 이원화 문제가 없었다). "선택한 채소" 풀(화분 미배정, `spaceId===""`)과 화분별 배치를 같은 rows 배열 안에서 spaceId 유무로만 나눠 별도 자료구조를 두지 않았고, 화분 안 순서는 기존 `position.order`(저장 순서) 개념을 그대로 재사용해 새 공간 데이터 모델을 만들지 않았다. 데스크톱은 포인터 이벤트를 직접 구현한 드래그(라이브러리 미도입, `planter-stage.tsx`의 기존 패턴 재사용), 모바일을 포함한 모든 환경은 탭-선택 → 탭-배치로 똑같이 동작한다. "자동 배치"는 미배정 채소만 호환 화분에 라운드로빈으로 채우고 이미 배치된 건 건드리지 않으며, "되돌리기"는 마지막 저장 상태로 한 번에 리셋한다(undo 스택 없음). 지지대 필요 여부·환경 적합도 같은 지능형 검증 문구는 작물 카탈로그에 해당 데이터가 없어 이번 범위에서 뺐다 — 화분 유형 비호환만 기존 `supportedSpaces`로 막는다. 화분 미배정 채소는 서버에 저장하지 않는 화면 임시 상태로 두었다(`ContainerPlacement.spaceId`가 필수라 저장할 방법이 없기도 하고, 스키마 변경 없이 가는 쪽을 택했다) — 저장 시 자동으로 배치에서 빠지며 그 전에 안내 배너로 알려준다. 크롬으로 기존 배치 데이터(상추·시금치)를 불러와 추가·선택·드래그·탭 배치·화분 비호환 거부·자동 배치·되돌리기까지 전부 직접 확인했다. 실제 저장 왕복은 로컬 `DEV_MOCK_AUTH` 환경이 모든 쓰기 요청을 204로만 흉내 내 확인하지 못했지만(의도된 동작, `route.ts` 주석에 명시), 저장 호출 자체는 기존 코드와 동일한 API 계약(`putContainerPlacements`)을 그대로 쓴다 — 2026-08-27
- [x] 진단 결과로 텃밭·시즌·화분 배치 자동 생성 — `/start` 진단은 sessionStorage에 `GardenConfiguration`을 남기지만, 그동안 `/spaces/new`는 화분 크기·햇빛만 폼에 미리 채워 보여줄 뿐 사용자가 이름을 짓고 작물을 처음부터 다시 골라야 했다("입력한 의미가 없다"는 사용자 지적). 이제 로그인/회원가입 직후 `/spaces/new`가 저장된 진단을 감지하면 폼을 보여주지 않고 공간→시즌→화분 배치를 순서대로 자동 생성해 배치 화면으로 바로 이동한다. 진단 추천은 `createGardenRecommendation`이 순수 함수라 재계산만으로 복원했다. 진단 작물 id(바질·딸기 등)가 실제 작물 도감과 100% 일치하지 않아 매핑표를 만들었고, 대응 작물이 없거나(바질·딸기) 만들어진 공간 유형이 지원하지 않는 작물(예: 실내의 토마토·고추)은 조용히 배치에서 제외한다. 실제로 화분이 하나도 안 채워지는 경우(추천 작물이 전부 매핑 실패) 이유 없이 빈 배치 화면으로 넘기지 않고 별도 안내를 보여주도록 후속 수정했다. 재시도 시 이미 만든 공간·시즌을 중복 생성하지 않는다 — 2026-08-26
- [x] 재배 도우미 챗봇 백엔드 — 팀원이 만드는 새 홈 화면의 AI 검색창·빠른 질문 칩(물 주기 시기/잎이 노래져요/햇빛 부족한가요) 3개에 대응하는 `POST /seasons/{season}/assistant/ask`. 자유 텍스트 질문은 이번 범위가 아니라 정해진 intent만 받는다. 새 테이블 없이 기존 `WateringSchedule`·`GrowingSpace`·`Crop.category`로 답하고, "물 줬어요" 액션은 기존 `CompleteWatering` 액션을 그대로 재사용했다(중복 구현 없음). 참고: 2026-08-25에 만든 `GET /growing-context`(공간·시즌·배치·기록 전체를 한 번에 주는 조회 전용 API)는 나중에 자유 텍스트 질문을 LLM에 붙일 때 컨텍스트로 쓰기 위해 준비해 둔 것으로, 이번 규칙 기반 칩 응답과는 별개 경로다. 자유 텍스트로 범위가 넓어지면 그 API를 재사용하는 쪽을 먼저 검토한다 — 2026-08-26
- [x] 랜딩 애니메이션 멈춤·화살표 버튼·모바일 겹침 3건 수정 — 사용자가 스크린샷으로 지적한 문제들. (1) 랜딩 씬 1(히어로)·씬 5(시작하기)의 GSAP 애니메이션이 스크롤 스냅으로 건너뛰거나 새로고침 시 스크롤 위치가 곧장 복원되면 재생을 놓쳐 텍스트가 영영 안 보이는 상태로 굳는 구조적 결함이었다 — 두 곳 모두 "재생을 놓쳤으면 직접 확인해서 대신 시작"하는 안전망을 추가했다. (2) "심어보기" 버튼의 숨은 화살표 아이콘이 `-150%`만 이동해, 아이콘 원이 작은 히어로 버튼(36px)에서는 안 보였지만 시작하기 섹션의 큰 버튼(44px)에서는 다 안 가려지고 겹쳐 보였다 — 이동 거리를 `260%`로 늘렸다. (3) `/start` 진단 화면 2곳의 모바일 레이아웃 겹침(화분 크기 입력 화면의 "가로" 줄이 배경과 겹침, 작물 선택 화면의 "03/03" 진행률이 로그인 버튼에 가려짐)과 랜딩 질문 장면이 터치로는 안 넘어가고 스크롤해야만 다음 질문으로 넘어가던 것(휴대폰에서 터치해도 한 화면 높이만큼 넘어가도록 추가)을 함께 고쳤다 — 2026-08-26
- [x] 진단 결과 화면 이모지 스타일 아이콘 교체 — 직전 점검에서 "실제 PNG 자산이라 이모지가 아니다"로 오탐 처리했던 `/start` 결과 화면의 흙·모종 아이콘을 사용자 요청으로 다시 확인했다. 실제로는 Unicode 이모지와 스타일이 사실상 동일한 광택 스티커형 일러스트였다(파일명은 실제 이미지지만 디자인 언어상 이모지에 해당). DESIGN.md "이모지 아이콘 금지" 규칙을 문자 형태가 아니라 이모지 스타일 자산 전반에 적용되는 규칙으로 다시 해석해, 작물과 무관한 일반 개념 아이콘(필요한 흙/모종) 2곳만 crop-catalog의 "색상 원 + 한글 이니셜" 대체 패턴과 동일한 텍스트 배지로 교체했다. 같은 화면에서 6종 작물마다 쓰는 crop-mark 스티커는 진단 기능 전체의 시각 자산 체계라 이번에는 그대로 두었다 — 2026-08-26
- [x] 실사용 흐름 점검 및 매끄러움 결함 4건 수정 — 회원가입부터 배치·일정·물주기·기록까지 크롬으로 QA 계정을 만들어 전체 흐름을 직접 돌아보며 발견한 결함을 고쳤다. (1) `/start` 3D 화분 진단이 "실내 화분" 조건에서 딸기·방울토마토 등 실제 카탈로그에 없거나 실내를 지원하지 않는 작물을 추천해, 진단을 보고 가입한 사용자가 정작 배치 화면(`/placements`)에서는 그 작물을 하나도 고를 수 없던 결함 — crops.json의 상추·시금치 supportedSpaces에 "indoor"를 추가(마이그레이션으로 동기화)하고, 진단의 crop-rules.ts에서 실내 카탈로그에 없는 바질·딸기·고추·방울토마토는 indoor를 allowedLocations에서 제거해 실제로 배치 가능한 작물만 추천하도록 정렬했다. (2) 화분 배치·텃밭 격자를 저장하면 서버에서 시즌 version이 올라가는데, 저장 API가 화면 전체가 공유하는 "seasons"·"layouts" 캐시를 갱신하지 않아 저장 직후 안내 링크로 재배 일정 화면에 가서 "일정 자동 만들기"를 누르면 옛 버전으로 412 충돌 오류가 났다(더블클릭 경합이 아니라 새로고침해야만 풀리는 별개 결함) — watering-api 등 기존 API가 쓰던 캐시 무효화 패턴을 container-placement-api·garden-layout-api에도 적용했다. (3) 화분·베란다 시즌은 작물을 이미 배치해도 "다음 추천 단계"가 계속 "화분 배치하기"만 보여주고, 대시보드 "작물 배치" 수치도 텃밭 격자 개수만 세고 있어 화분 배치는 항상 0개로 표시됐다 — 두 로직 모두 실제 배치 여부를 반영하도록 고쳤다. (4) 약 850~1300px 폭(작은 노트북 화면)에서 헤더의 "공간 추가" 버튼과 계정 메뉴 글자가 중간에서 줄바꿈되던 문제를 내비게이션이 먼저 가로 스크롤되도록 고쳐 해결했다. 학교 서버에 배포해 네 가지 모두 실제 계정으로 재현·수정 확인을 마쳤다 — 2026-08-26
- [x] 더블클릭 경합 조건 전수 조사 및 방지 — `cultivation-schedule.tsx`에서 먼저 고쳤던 "isSaving state만으로는 중복 클릭을 못 막는다"는 패턴(state 반영이 커밋 전에 두 번째 클릭이 들어오면 옛 버전으로 중복 요청 발송 → 412 충돌 배너)을 프론트 전체에서 재조사했다. 실제로 같은 결함이 있던 9개 파일(메모 추가·삭제, 시즌·공간 삭제, 격자 생성·칸 토글·격자 초기화·시즌 기간 제안 적용 4곳, 재배 기록 관리, 화분 배치 저장, 물주기 관리, 회원가입 제출, 시즌 등록·수정 제출)에 동일한 `useRef<boolean>` 동기 잠금을 적용했다(시즌 등록·수정 폼은 잠금 자체가 아예 없어 버튼 비활성화도 함께 추가). 버전 충돌 위험이 없는 읽기 전용 조회(주소 검색·현재 위치)와 애초에 잠금 상태 자체가 없는 별개 이슈(로그인 폼)는 이번 조사 범위에서 제외했다 — 2026-08-25
- [x] 요금제 화면 진입 경로 추가, 지지대 필요 작물 재배 일정 항목 추가 — `/plans`(구독 화면)는 완성돼 있었지만 인증 사용자 UI 어디에도 링크가 없어 URL을 직접 입력해야만 도달할 수 있었다(발견 불가능 상태) — 헤더 네비게이션에 "요금제" 항목을 추가했다. 재배 일정이 파종·수확 두 단계뿐이라 부족하다는 지적에 대해서는, `needs_support=true`인 작물(토마토·오이·고추·강낭콩 4종)마다 지지대 설치 항목(`CultivationTaskType::Support`, 이전엔 enum만 정의되고 미사용)을 심는 날짜와 같은 날 생성하도록 했다 — crops.json에 "심은 후 N일" 같은 간격 데이터가 없어 임의 날짜를 지어내지 않고 이미 계산된 심는 날짜를 그대로 재사용했다. 비료 일정과 꽃 3종의 상시 주의사항(찬물 직접 닿지 않게 등, 날짜 없는 항목)은 특정 시점 데이터가 없어 이번에도 일정화하지 않았다 — 2026-08-25