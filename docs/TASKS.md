# 심어봄 작업 목록

기준일: 2026-08-25

이 문서는 아직 끝나지 않은 작업과 바로 이어서 할 순서를 관리하는 단일 기준입니다. 구현 상태 설명은 [PROJECT_STATUS.md](PROJECT_STATUS.md), 진행 중 변경의 상세 인계는 [HANDOFF.md](HANDOFF.md)를 확인합니다.

## 목록 관리 규칙

- `[ ]` 대기, `[-]` 진행 중, `[!]` 막힘, `[x]` 완료 상태를 사용합니다.
- 작업을 시작한 즉시 항목을 `[-]`로 바꾸고 브랜치 이름을 적습니다.
- 검증까지 끝난 작업은 같은 작업 안에서 즉시 `[x]`로 바꾸고 완료일을 적거나, 이후 인계 가치가 없으면 목록에서 제거합니다.
- 완료된 작업을 `[ ]`, `[-]`, “다음 작업”, “남은 작업” 상태로 남겨 두지 않습니다.
- 작업 상태를 바꾼 코드와 이 문서의 상태 변경은 같은 기능 커밋에 포함합니다.
- 진행 목록은 이 문서에만 두고 `README.md`, `PROJECT_STATUS.md`, `HANDOFF.md`에는 중복 목록 대신 이 문서 링크를 둡니다.
- 완료 기록은 최근의 중요한 항목만 최대 10개 유지합니다. 오래된 완료 이력은 Git 기록과 `HANDOFF.md`의 완료 표로 확인합니다.
- 관련 테스트·정적 검사·빌드를 통과하기 전에는 `[x]`로 표시하지 않습니다.

## 진행 중

## 다음 작업

## 최근 완료

- [x] 요금제 화면 진입 경로 추가, 지지대 필요 작물 재배 일정 항목 추가 — `/plans`(구독 화면)는 완성돼 있었지만 인증 사용자 UI 어디에도 링크가 없어 URL을 직접 입력해야만 도달할 수 있었다(발견 불가능 상태) — 헤더 네비게이션에 "요금제" 항목을 추가했다. 재배 일정이 파종·수확 두 단계뿐이라 부족하다는 지적에 대해서는, `needs_support=true`인 작물(토마토·오이·고추·강낭콩 4종)마다 지지대 설치 항목(`CultivationTaskType::Support`, 이전엔 enum만 정의되고 미사용)을 심는 날짜와 같은 날 생성하도록 했다 — crops.json에 "심은 후 N일" 같은 간격 데이터가 없어 임의 날짜를 지어내지 않고 이미 계산된 심는 날짜를 그대로 재사용했다. 비료 일정과 꽃 3종의 상시 주의사항(찬물 직접 닿지 않게 등, 날짜 없는 항목)은 특정 시점 데이터가 없어 이번에도 일정화하지 않았다 — 2026-08-25
- [x] 헤더 위치 고정, 화분 물주기 허용, 일정 완료 경합 방지, 시즌 기간 추천 — 사용자가 화면을 직접 써보며 짚어준 문제들을 코드로 확인하고 고쳤다. (1) 헤더 네비게이션이 페이지마다 살짝 밀리던 것은 `justify-content: space-between` 3분할 레이아웃이 페이지별 액션 버튼 길이에 영향받던 게 원인이라 3단 그리드(1fr auto 1fr)로 고쳤다(DOM 좌표로 페이지 간 완전히 동일함을 확인). 로고는 항상 "/"로 갔다가 로그인 상태를 다시 확인해 리다이렉트되는 중간 화면을 거쳤는데, 헤더가 직접 인증 상태를 읽어 /dashboard 또는 /start로 바로 이동하게 했다. "작물관리"와 완전히 같은 목적지였던 돋보기 버튼은 제거했다. (2) 화분·베란다는 물주기 기능 자체가 막혀 있었다(프론트는 space.type!=='garden'이면 차단, 백엔드 CreateWateringSchedule도 garden_layout_placements만 조회) — 실제 재배자에게는 물을 줘야 하는데 만들 방법이 없던 진짜 결함이었다. 백엔드는 container_placements도 함께 조회하도록 넓히고, 프론트는 공간 유형에 따라 배치 출처를 나눠 화분 배치 화면도 안내하게 했다. 학교 서버에서 실제로 화분 시즌에 물주기 일정을 만들어 확인했다. (3) 재배 일정 완료 버튼을 아주 빠르게 두 번 누르면 disabled 반영 전에 같은 옛 버전으로 요청이 중복 발송돼, 실제로는 완료됐는데도 412 충돌 배너만 남는 경우가 있었다 — isSaving state만으로는 못 막아 동기 잠금 ref를 추가했다. (4) 작물 상세에서 시즌을 만들 때 권장 심기·수확 시기를 안내만 하고 날짜는 손으로 입력해야 했다 — `suggestSeasonPeriodForCrop`이 오늘 이후 가장 가까운 권장 기간을 계산해 버튼 하나로 시작일·종료일을 한 번에 채운다(연말을 넘기는 권장 시기는 제안하지 않음). 학교 서버에서 실제 계정으로 네 가지 모두 동작을 확인했다 — 2026-08-25
- [x] 시즌 기간 제안 즉시 적용 및 삭제 차단 화면에 바로가기 링크 추가 — 사용자가 실제로 겪은 두 가지 불편을 고쳤다. (1) 텃밭 배치 화면에서 배치한 작물의 권장 시기가 시즌 기간과 맞지 않을 때, 그동안은 "시즌 기간 수정하기"로 별도 화면에 보내 날짜를 직접 다시 입력하게 했다. `suggestSeasonPeriodCoveringCrops`가 문제되는 작물들의 심는·수확 권장 달을 모두 포함하는 기간을 계산해 보여주고, 버튼 하나로 그 자리에서 바로 적용한다(페이지 이동 없음). 권장 시기가 연말을 넘기는 작물이 섞이면 하나의 기간으로 묶을 수 없어 제안을 생략한다. (2) 공간·시즌 삭제가 "먼저 OO를 삭제해야 합니다"로 막힐 때 그 OO가 어느 화면에 있는지 알려주지 않던 것을, 서버 오류 코드(SPACE_HAS_SEASONS, SEASON_HAS_TASKS 등)에 맞춰 해당 화면으로 가는 링크를 오류 메시지 옆에 붙였다. 학교 서버에서 실제 계정으로 두 기능 모두 동작을 확인했다 — 2026-08-25
- [x] 리소스 Policy 비대칭 정리 — `CultivationTask`·`CultivationRecord`·`WateringSchedule`·`SpaceMemo` 4개에 전용 Policy(`CultivationTaskPolicy` 등, `GrowingSeasonPolicy`와 동일하게 view/update/delete가 모두 owner_id 비교로 귀결되는 구조)를 새로 만들고, 이미 존재하는 인스턴스를 다루는 곳(수정·삭제·조회·완료·미루기·되돌리기 등)은 부모(`growingSeason`/`growingSpace`)가 아니라 모델 자체(`Gate::authorize('update', $task)`)를 인가하도록 옮겼다. `GardenLayout`·`ContainerPlacement`는 대상에서 제외했다 — 두 리소스는 URL에 자기 자신의 id가 없는 시즌 종속 단일/집합 자원이라(`/seasons/{id}/layout`, `/seasons/{id}/container-placements`) 컨트롤러에 애초에 모델 인스턴스가 없고, 생성 시점엔 인스턴스 자체가 없어 부모(시즌)를 인가하는 지금 구조가 맞다. 새로 생성하는 시점(아직 인스턴스가 없는 Store류)은 그대로 부모를 인가한다 — 소유권 판정 로직은 한 비트도 바꾸지 않은 순수 구조 정리라 전체 백엔드 208개 테스트가 코드 변경 없이 그대로 통과했다 — 2026-08-25
- [x] 관리자 콘솔 테스트 공백 채우기 — `AdminCatalogController`(`/admin/catalog`)와 `AdminUserController::index`(`/admin/users`)가 테스트 스위트에 전혀 없던 것을 채웠다. `AdminCatalogTest`(비로그인·일반 회원 접근 차단, 관리자에게 실제 작물·출처 데이터 노출)와 `AdminUserIndexTest`(접근 제어, 닉네임/이메일 검색, 상태 필터)를 새로 추가했다. 검색·필터 모두 정상 동작해 실제 버그는 없었다 — 순수 테스트 추가, 프로덕션 코드 변경 없음 — 2026-08-25
- [x] 물주기(watering) 인가 체크 위치 통일 — 코드 조사 결과 실제로는 3가지 패턴이 섞여 있었다: 물주기 4개(FormRequest만 체크), 메모·기록 Store/Update(FormRequest와 컨트롤러 둘 다 체크, 중복), 시즌 Store/Update·작업 Update·작업 일괄삭제·기록 사진 업로드(컨트롤러만 체크, `FormRequest::authorize()`는 무조건 `true`). 본문을 검증하는 모든 엔드포인트(Store/Update류)가 `FormRequest::authorize()`에서 소유권을 확인하도록 통일하고, 컨트롤러의 중복·전담 `Gate::authorize()` 호출을 제거했다 — 본문이 없는 삭제류 엔드포인트는 FormRequest 자체가 없어 그대로 컨트롤러에서 확인한다(구조상 불가피, 대상 아님). 시즌의 `spaceId`처럼 URL이 아니라 요청 본문으로 전달되는 대상 ID는 `Str::isUuid()`로 형식을 먼저 걸러 형식 오류는 그대로 422로, 존재하지 않는 ID는 그대로 404로 유지했다(권한 확인을 앞당기면서 기존 오류 코드 순서를 바꾸지 않음). 이번 통일로 인가 검사가 검증보다 먼저 실행되도록 일관돼, 다른 사용자가 잘못된 입력을 보내도 검증 오류(422) 대신 곧바로 403이 반환된다. 새로 옮겨진 4곳(시즌 Store/Update, 작업 Update, 작업 일괄삭제)에 다른 소유자 접근 테스트를 추가했다 — 2026-08-25
- [x] 구독 연체 재시도 정책 및 관리자 구독 현황 화면 — 결제 실패 시 `ChargeDueSubscriptions`가 연체(`past_due`) 구독을 다시는 조회하지 않아 영구 방치되던 문제를 고쳤다(문서화되지 않았던 결함, 코드 조사로 발견). `RecordFailedSubscriptionCharge`가 3일 간격 최대 3회 재시도 정책을 관리하고, 스케줄러(`ChargeDueSubscriptions`)와 토스 웹훅(`ReconcileSubscriptionWebhookEvent`) 두 실패 경로가 이 정책을 공유한다 — 이전에는 웹훅 경로가 재시도 카운트 없이 곧바로 `past_due`로만 바꿔 스케줄러 조회 대상에서 영구히 빠지는 별도의 구멍이 있었다. 재시도 중 성공하면 즉시 `active`로 복귀(재시도 카운트 초기화), 3회 모두 실패하면 자동으로 `canceled` 처리하고 `SubscriptionCanceledMail`을 큐로 발송한다. 관리자 콘솔에 `/admin/subscriptions`(구독자 목록·상태·다음 결제일 또는 해지일·재시도 횟수·다음 재시도 시각·최근 결제 실패 사유, 상태 필터)를 새로 추가했다 — 이전에는 연체 회원을 볼 화면 자체가 없었다. `subscriptions` 테이블에 `past_due_retry_count`·`next_retry_at` 컬럼 추가(nullable/default, 기존 행 영향 없음) — 2026-08-25
- [x] 로그인 상태별 첫 진입 경로 분기 — 루트(`/`)가 더 이상 정적 랜딩 화면을 보여주지 않고 로그인 상태를 확인해 비로그인은 `/start`(시작 진단)로, 로그인 상태는 `/dashboard`(내 텃밭 홈)로 즉시 이동한다. `/start`에도 `GuestGate`를 추가해 이미 로그인한 사용자가 네비게이션의 "심어봄 클래스" 링크 등으로 들어와도 진단 화면 대신 바로 `/dashboard`로 보낸다(기존에는 화면 안의 작은 "내 텃밭으로" 링크를 직접 눌러야만 빠져나갈 수 있었다). 기존 홈 랜딩(`HomeDashboard`) 컴포넌트는 라우트에서 더 이상 쓰이지 않지만 삭제하지 않고 남겨 두었다. 크롬으로 전체 흐름(익명 진단→회원가입 사전 채움→공간·시즌·배치·일정 생성→홈 카드 표시)을 처음부터 다시 확인하다가 화분 배치 작업 때 남아 있던 "대표 작물을 선택합니다" 관련 안내 문구 2곳(`space-form.tsx`, `space-list.tsx`)도 함께 고쳤다 — 2026-08-25
- [x] AI 질문용 재배 정보 조회 API — `GET /growing-context`(로그인 사용자의 공간·시즌·화분 배치·최근 기록 최대 20건·메모·완료 안 된 일정 최대 20건을 한 번에 반환, `GrowingContext` 도메인). 새 테이블 없이 기존 모델을 owner 기준으로 조회만 한다. 응답은 기존 `GrowingSpaceResource`·`GrowingSeasonResource`·`ContainerPlacementResource`·`GardenLayoutResource`·`CultivationRecordResource`·`SpaceMemoResource`·`CultivationTaskResource`를 그대로 재사용해 조합했다(새 필드 형식을 만들지 않음). AI 제공자·모델은 여전히 미정이라 이 세션에서 실제 AI 연동·화면은 만들지 않았다 — 읽기 전용 백엔드 엔드포인트만 완성했고, 실제 소비처(AI 질문창)는 제공자가 정해진 뒤 별도 작업 — 베란다 화분 재배 계획 확장 로드맵 전체 완료 — 2026-08-25
- [x] 화분 배치 프론트 화면 — `PUT /seasons/{id}/container-placements`를 쓰는 목록형 배치 화면(`/seasons/{id}/placements`, `container-placement` 도메인)을 새로 만들었다. 행마다 화분·작물·수량을 고르며, 한 시즌 안에서 화분을 여러 개 선택할 수 있어 "여러 화분을 하나로 묶는 계획" 요구사항을 만족한다. 좌표·격자는 쓰지 않고(`frontend/DESIGN.md`의 화분·베란다 무격자 방침 유지) `position`엔 표시 순서만 저장한다(서버가 `id`로 정렬해 반환하므로 순서 보존에 필요). 기존 "대표 작물 선택" 드롭다운은 화분·베란다 시즌에서 완전히 제거해 이 화면으로 교체했다(텃밭의 "대표 작물 (선택)"은 그대로 유지). 시즌 생성 시 화분·베란다에 대표 작물을 강제하던 백엔드 검증도 함께 제거했다(`featuredCropId`는 배치 이전 시즌과의 하위 호환용으로만 남음). 시즌 목록의 "다음 추천 단계"는 화분·베란다에서 항상 배치 화면으로 안내하도록 단순화했다(화분별 저장 여부를 목록 화면에서 미리 조회하면 N+1이 생겨, 정확한 다음 단계 안내는 배치 화면 진입 후에 보여준다). 학교 서버에서 실측하며 `featuredCropId`에만 의존하던 다운스트림 3곳도 함께 고쳤다: 재배 일정 생성 화면(배치가 있어도 "작물을 먼저 선택하라"며 막던 결함), 대시보드 다음 행동 추천, 홈 화면의 "관리 식물" 카드·대표 작물 계산. 홈·대시보드는 시즌마다 배치를 따로 조회하면 N+1이 생겨 `GET /container-placements`(로그인 사용자의 배치 전체, `GET /layouts`와 같은 패턴)를 새로 추가했다 — 베란다 화분 재배 계획 확장의 마지막 단계 — 2026-08-25
