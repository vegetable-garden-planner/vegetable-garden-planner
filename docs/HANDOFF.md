# 심어봄 작업 인계서

기준일: 2026-08-28 (화분 배치 그리드 캔버스 재구성 완료 직후)

이 문서는 새 팀원이나 AI가 이전 대화를 보지 않아도 현재 상태를 파악하고 바로 작업을 이어가기 위한 실행 중심 문서입니다. 작업을 시작할 때는 [프로젝트 작업 가이드](README.md)를 먼저 읽고, 그다음 이 문서를 읽습니다.

## 1. 한눈에 보는 현재 상태

- 안정 기준 브랜치: `develop`, 최신 커밋 `6009302`(2026-08-28) — 정확한 커밋은 작업 시작 시 `git log -1 --oneline develop`로 확인
- **방금 끝낸 작업(가장 중요, 이 세션 전부)**: 화분 배치("작물 배치") 화면을 팀원이 만든 배치 편집기 프로토타입 기준으로 통째로 재구성했다. 상세는 아래 "5. 현재 진행 상태"와 `TASKS.md` 최근 완료 맨 위 항목, 로컬 Claude Code 메모리 `project_placement_grid_canvas.md` 참고. **다음 세션은 바로 이 작업의 후속(그룹 상자, 모바일 바텀시트)부터 시작하면 된다** — 아래 "11. 다음 AI가 바로 실행할 순서"가 이미 그 순서로 갱신돼 있다.
- 프론트 운영: https://vegetable-garden-planner.vercel.app — `develop` push 시 자동 배포(반영 여부는 실제로 열어서 확인)
- 백엔드 운영(닷홈, https://yjwest9.dothome.co.kr): **사용자 지시로 이 단계에서는 관리·확인 대상이 아니다(실사용자가 생기기 전까지).** FileZilla 수동 업로드 방식이라 develop과 자동으로 동기화되지 않으며, 최근 여러 사이클의 백엔드 변경이 반영됐는지 확인되지 않은 채로 남아 있다. 사용자가 먼저 요청하기 전에는 닷홈 반영 여부를 점검하거나 언급하지 않는다.
- 학교 테스트 서버(스테이징, 사실상 유일한 배포 대상): `wa26b02.yjjob.kr` — Apache가 프론트(Next.js, pm2)와 백엔드(Laravel)를 한 도메인에서 같이 서비스. 실제 사용자 흐름을 브라우저로 확인할 때 이 서버를 쓴다. `develop@09eccd6`까지 배포·마이그레이션(새 마이그레이션 없음)·빌드·스모크 테스트(`/`, `/api/v1/health` 200 확인) 완료. **랜딩 애니메이션이 실제로 재생되는지는 사람이 직접 확인 안 됨** — 이 세션의 브라우저 자동화 창이 화면에 떠 있지 않은 상태로 고정돼 있어 `requestAnimationFrame`이 안 돌아 GSAP 애니메이션 완료 여부를 AI가 확인할 방법이 없었다(코드 로직은 로컬에서 디버그 로그로 검증). SSH 접속 정보(호스트·계정·비밀번호)는 이 문서에 적지 않는다 — 로컬 Claude Code 메모리(`project_school_server_https.md`, 이 컴퓨터에만 있고 git에는 없음)에 있으니 그 문서를 먼저 확인한다. 상세한 Apache 프록시 설정·트러블슈팅 이력도 같은 메모리 파일에 있다.
- 화분·텃밭 배치 결과 화면 Frame 12 재작업(2026-08-29 develop 병합·배포·시각 검증 완료): 화분(container)형·격자(grid)형 결과 화면 둘 다 Frame 12 시안과 일치함을 wa26b02에서 확인했다. 상세는 `TASKS.md` 최근 완료 참고. 원래 사용자가 지적한 3가지 문제 중 남은 건 **작물 상세 페이지(Frame 2) 시각 재작업**(아직 착수 전, 다음 우선순위) 하나뿐이다.
- 홈 화면·마이페이지 분리, 전역 사이드바 도입(2026-08-29 develop 병합·배포·시각 검증 완료): 프로젝트 폴더 안 팀원 디자인(`frontend-develop`)을 기준으로 반영했다. 헤더 평면 메뉴 → 전역 사이드바(`AppSidebar`)로 교체, `/dashboard`는 "지금 무엇을 키우는지"만 보여주는 캐러셀(`HomeStage`)로 단순화, 예전 `/dashboard`의 상세 목록은 새 `/mypage`로 이동. **주의**: `frontend-develop` 폴더 안에 실행 불가능한 부분 스냅샷(`simeobom-frontend-final`)이 중첩돼 있고, 이름과 달리 이게 최신이 아니었다(파일 타임스탬프로 확인 필요, 바깥쪽이 8시간 더 최신이었음) — 이 폴더를 다시 참고할 일이 있으면 매번 타임스탬프로 최신 여부를 다시 확인할 것, 폴더/파일 이름만 믿지 않는다. 상세는 `TASKS.md` 최근 완료 참고.
- 홈 화면 검색창(2026-08-29 develop 병합·배포·시각 검증 완료): `frontend-develop-v2`(팀원이 "수정본"이라고 넣어준 새 스냅샷)에서 `HomeSearchBar`를 반영했다. **AI 검색이 아니다** — 이미 불러온 내 재배 계획·공간·작물 도감 데이터 안에서만 찾아 이동하는 로컬 검색이고, 코드 주석에 "자연어 질문에 답하는 AI 기능은 보류 상태"라고 명시돼 있다. `placement-studio`(새 화분 배치 캔버스 화면)와 `seasons/[id]/{layout,placements}` 페이지 변경은 아직 확인 안 돼서 건너뛰었다 — 다음에 `frontend-develop-v2`를 다시 볼 때 이어서 처리. 상세는 `TASKS.md` 최근 완료 참고.
- 재배 도우미 챗봇(2026-08-26 intent 기반, 2026-08-28 자유 텍스트+Gemini 추가): 백엔드는 완성됐지만, 새 홈 화면(`HomeStage`, 위 항목)에는 아직 AI 질문 입력창이 연결돼 있지 않다(위에서 반영한 검색창도 AI가 아닌 로컬 검색 — 실제 AI 질문 입력창은 여전히 없다). 두 경로가 공존한다 — (1) `POST /seasons/{season}/assistant/ask`: 화면 칩 전용, seasonId 필수, intent 4종 중 하나를 정확히 받아야 함(`log_watering`은 실제로 물주기를 완료 처리). (2) `POST /ai/chat`: 팀원이 검색창용으로 요청한 자유 텍스트 경로, `{"message": "..."}` → `{"data": {"answer": "..."}}`. 하이브리드로 동작한다 — `ClassifyAssistantQuestion`(키워드 매칭)으로 정해진 3가지 질문(물주기 시기·잎 노래짐·햇빛 부족)이면 (1)의 무료 규칙 기반 로직으로 즉답하고, 그 외 자유 텍스트는 `AskGenerativeAssistant`가 **Gemini API**(무료 등급, `GEMINI_API_KEY` 필요)로 실제 생성형 답변을 받아온다 — `GET /growing-context`(2026-08-25 완성)의 데이터를 시스템 프롬프트 컨텍스트로 넣고, 사용자 데이터에 없는 구체적 수치·날짜는 지어내지 말라는 가드레일을 넣었다. `GEMINI_MODELS`(쉼표 구분, 기본값은 무료 등급이 있는 텍스트 생성 모델 8개를 한도가 넉넉한 순서로 나열 — `backend/config/services.php` 참고)에 나열한 순서대로 시도하며 앞 모델이 할당량 초과(429)면 자동으로 다음 모델로 넘어간다. 등록된 재배 공간이 아예 없으면 Gemini를 부르지 않고 안내 문구로 끝내 비용을 아낀다. **`GEMINI_API_KEY`는 로컬 `backend/.env`와 wa26b02 서버 `.env`에 각자 채워 넣어야 실제로 동작한다(git에 없음, 키가 없으면 항상 재시도 안내 문구만 나온다)** — Google AI Studio(aistudio.google.com/apikey)에서 발급. wa26b02 서버 `.env`에는 이미 채워 넣고 임시 QA 계정으로 실제 응답 확인까지 마쳤다(2026-08-28). 데이터를 바꾸는 `log_watering`은 오분류 위험 때문에 이 경로 어디서도 자동 실행하지 않는다. 비용 안전장치로 라우트 throttle을 분당 10회로 보수적으로 잡아뒀다(사용량 한도는 추후 재조정 예정). 상세는 `TASKS.md` 최근 완료와 `openapi.yaml`의 `AiChat` 태그 참고.
- 학교 서버 결제 설정(2026-08-26 적용): 토스페이먼츠 키가 양쪽 `.env`에 모두 비어 있어 `/plans`의 "프로 구독하기"가 "결제 설정(토스페이먼츠 clientKey)이 되어 있지 않습니다."로 막혀 있었다. 공식 **문서용 테스트 키**(회원가입·계약 불필요, 빌링 지원, 실제 출금 없음)를 넣어 두었다 — 백엔드 `~/laravel_app/backend/.env`에 `TOSS_PAYMENTS_SECRET_KEY`·`TOSS_PAYMENTS_BASE_URL`(`config:clear` 완료), 프론트 `~/laravel_app/frontend/.env.production.local`에 `NEXT_PUBLIC_TOSS_PAYMENTS_CLIENT_KEY`. 수정 전 두 파일 모두 `.bak.20260826_131147`로 백업했다. **프론트 키는 빌드 시점에 번들로 박히므로 다음 프론트 빌드 전까지는 브라우저에 반영되지 않는다.** 키 값과 근거 URL은 [LOCAL_DEVELOPMENT.md](LOCAL_DEVELOPMENT.md)의 "결제(토스페이먼츠) 테스트 키 설정"에 있다. `TOSS_PAYMENTS_WEBHOOK_SECURITY_KEY`는 계약 상점에만 발급되므로 비어 있고, 그 결과 웹훅 수신만 서명 검증에서 거부된다(카드 등록·구독 생성·정기결제는 정상).
- 프론트 Production 브랜치: `develop`
- 프론트 Root Directory: `frontend`
- DB 스키마 단일 기준: `backend/database/migrations`

현재 기능은 회원가입부터 공간·시즌·작물 배치(텃밭 격자/화분 목록 배치)·재배 일정·물주기·재배 기록·시즌 기록 사진·웹 푸시 알림·프로 요금제 결제(토스페이먼츠 빌링)·관리자 콘솔(회원·구독·작물 카탈로그)까지 Laravel API와 MySQL에 연결되어 있다. 상세한 구현 완료 목록은 [PROJECT_STATUS.md](PROJECT_STATUS.md)를 확인한다(이 문서와 중복 기술하지 않음).

진행 중 작업과 다음 순서는 [작업 목록](TASKS.md)이 단일 기준입니다. 작업을 완료하면 같은 작업 안에서 즉시 완료 표시하거나 목록에서 제거합니다. `TASKS.md`의 "최근 완료"에 최근 10개 작업의 상세한 배경과 이유가 있으니, 이 문서와 별도로 꼭 읽는다.

## 2. 반드시 지킬 Git 규칙

1. 작업 전에 `docs/README.md`와 해당 영역 문서를 끝까지 읽습니다.
2. `develop`과 원격이 같은지 확인한 뒤 기능마다 새 `feature/*` 브랜치를 만듭니다.
3. 원격에 같은 이름의 브랜치가 있을 수 있으므로 시작점을 명시합니다.

   ```powershell
   git switch develop
   git pull --ff-only origin develop
   git switch -c feature/<고유한-기능명> origin/develop
   ```

4. 기능·정리·병합 커밋 메시지는 모두 한글로 작성합니다.
5. 관련 검증과 전체 검증을 통과한 뒤 `--no-ff`로 `develop`에 병합하고 push합니다.
6. 병합된 로컬 기능 브랜치는 삭제합니다.
7. `feature/yj-work`와 `origin/feature/yj-work`는 팀원 작업 브랜치이므로 수정·병합·삭제하지 않습니다.
8. `origin/feature/growing-space-design-refresh`는 이미 존재하며 디자인 원본 이미지 커밋을 포함한 원격 브랜치입니다. 같은 이름으로 새 브랜치를 만들면 Git이 해당 원격 브랜치를 시작점으로 추정할 수 있으므로 재사용하거나 임의로 수정하지 않습니다.
9. 사용자의 기존 변경이나 팀원 커밋을 임의로 되돌리지 않습니다.

## 3. 지금까지 완료한 기능

### 인증과 관리자

- Laravel 세션 기반 회원가입·로그인·로그아웃과 회원가입 직후 자동 로그인
- 이메일 중복 확인, 비밀번호 조건과 확인 검증
- Google·카카오 소셜 로그인, 검증 이메일 기반 기존 계정 연결, 제공자별 실패 안내
- 관리자 별도 로그인, 운영 대시보드, 회원 관리, 작물 기준정보·출처 확인

### 재배 공간과 시즌

- 실내 화분, 베란다, 마당·텃밭 공간 CRUD
- 카카오 주소 검색과 현재 위치 사용
- 위도·방향을 이용한 예상 직사광 시간 계산
- 공간별 시즌 CRUD, 기간·중복 검증
- 화분·베란다 대표 작물 선택과 지원 공간 검증

### 공간 유형별 핵심 흐름

- 마당·텃밭: 시즌 생성 → 격자 생성 → 작물 배치 → 재배 일정 생성
- 화분·베란다: 시즌 생성과 대표 작물 선택 → 격자 없이 재배 일정 생성
- 화분·베란다가 격자 URL에 직접 접근하면 텃밭 전용 기능임을 설명하고 작물 선택 또는 일정 생성으로 이동
- 화분·베란다에 격자를 다시 적용하지 않습니다.

### 일정과 기록

- 작물 기준정보와 권장 심기·수확 시기 표시
- 텃밭 격자 배치, 간격·예상 포기 수·연작 위험 안내
- 작물별 재배 일정 자동 생성과 완료·되돌리기·수정·삭제
- 물주기 일정 CRUD, 완료·미루기·완료 취소와 이력
- 작업·성장 관찰·수확·물주기 시즌 기록 CRUD
- 대시보드 일정 알림과 공간 유형에 맞는 다음 단계 안내

### API 문서와 작업 관리

- `backend/resources/openapi.yaml`을 단일 기준으로 사용하는 Swagger UI
- 로컬 `/api-docs`, 운영 배포 후 `https://yjwest9.dothome.co.kr/api-docs`
- Laravel이 제공하는 `/api-docs/openapi.yaml` 명세 응답과 회귀 테스트
- 진행·대기·완료 상태를 즉시 갱신하는 [작업 목록](TASKS.md)

## 4. 완료된 디자인 개편

디자인 기준은 [프론트 디자인 시스템](../frontend/DESIGN.md)입니다. 원본 시안의 핵심은 짙은 온실 사진, 에메랄드 포인트, 따뜻한 흰색 배경, 얇은 경계, 둥근 카드, 긴 세로 스크롤 구성입니다.

| 영역 | 상태 | 관련 커밋 |
| --- | --- | --- |
| 대시보드 재배 계획 구조 | 완료 | `41f84fb` |
| 단일 시즌 대시보드 여백 보정 | 완료 | `70fc5ac` |
| 작물 배치 상세 작업 화면 | 완료 | `d8a6e5e` |
| 재배 일정 화면 | 완료 | `1f6cda2` |
| 재배 공간 목록·등록·수정 | 완료 | `f5fec07` |
| 시즌 목록·등록·수정 | 완료 | `b1081dc` |
| 물주기 일정·완료·미루기·이력 | 완료 | `9ad46af` |
| 시즌 재배 기록 목록·등록·수정 | 완료 | `93c2835` |
| 작물 목록·검색·필터·상세 | 완료 | `0db50e5` |
| 로그인·회원가입과 카카오 로그인 | 완료 | `9160f83` |
| 시작 진단과 요금제 | 완료 | `feature/claude-start-plans-design-refresh` |

완료 화면은 기존 API 기능을 유지하면서 데스크톱과 모바일 레이아웃을 모두 확인했습니다. Swagger 작업까지 포함한 현재 전체 검증 결과는 다음과 같습니다.

- 프론트엔드 테스트: 123개 통과
- 백엔드 테스트: 118개, 780 assertions 통과
- TypeScript 타입 검사 통과
- ESLint 통과
- Pint 통과
- Next.js 프로덕션 빌드 통과

## 5. 현재 진행 상태

**가장 최근 사이클(이번 세션, 2026-08-28) — 화분 배치 그리드 캔버스 재구성:**

사용자가 발표 자리에서 배치 화면이 팀원이 만든 것과 달라 곤란했다며, 팀원의 실제 작동 프로토타입(`file://192.168.0.67/...업로드서버주소/심어봄_배치편집기_그룹리사이즈_전체분석 (1).html`, UNC 경로)을 직접 열어보고 그걸 기준으로 다시 만들라고 지시했다. "시간 많이 써도 되니 하나하나 클릭해서 확인하라"는 명시적 지시에 따라 wa26b02 실서버에서 QA 계정으로 끝까지 클릭 검증했다.

- 화분(재배 공간)을 10cm 단위 격자로 그리고 작물을 칸 단위로 배치하는 캔버스로 교체(`ContainerGridCanvas`, `container-placement-grid.ts`). 새 DB 테이블 없이 기존 `position` JSON을 `{order}` → `{order, cells}`로 확장.
- 화분 깊이·햇빛·이웃 간격 3가지를 실제 데이터로 검증해 적합/주의/부적합 배지 표시, 빈 칸 중 점수 높은 2칸 추천 하이라이트.
- 다른 작물이 이미 차지한 칸과 겹치지 않는 충돌 방지 로직.
- **범위에서 뺀 것(사용자와 합의)**: 그룹 상자(비영구 화면 상태), 재배일지·메모(새 DB 테이블 필요), AI 자연어 편집, 생육 반경 표시(뒷받침 데이터 없음), **모바일 바텀시트 레이아웃**(이 환경 브라우저 자동화 도구가 뷰포트 리사이즈를 스크린샷에 반영 못해 이번엔 시각 확인 자체가 불가능했음 — 다음 세션에서 다른 방법으로 확인 필요).
- 프론트 테스트 215개·typecheck·lint·build 통과, `develop` 병합·wa26b02 재배포 완료. 상세는 `TASKS.md` 최근 완료 맨 위 항목과 로컬 메모리 `project_placement_grid_canvas.md` 참고.

**바로 이전 사이클(2026-08-29 표기, 회원가입~배치~일정~기록 전체 흐름 QA):**

상세 배경·판단 근거는 [TASKS.md](TASKS.md)의 "최근 완료" 항목이 이 문서보다 훨씬 구체적이니 그쪽을 우선 읽습니다. 여기서는 다음에 이어받을 사람이 알아야 할 상태만 요약합니다.

**이 사이클에서 실제로 고친 것 (모두 `develop` 병합·검증·학교 서버 배포까지 완료):**
- `/start` 3D 화분 진단이 "실내 화분" 조건에서 실제 카탈로그에 없거나(바질·딸기) 실내를 지원하지 않는(고추·방울토마토) 작물을 추천해, 진단을 보고 가입해도 실제 배치 화면에서는 그 작물을 하나도 고를 수 없던 근본적인 온보딩 단절 — 상추·시금치의 `supportedSpaces`에 `indoor`를 추가(신규 마이그레이션으로 동기화)하고 진단 로직도 실제로 배치 가능한 작물만 추천하도록 정렬
- 화분 배치·텃밭 격자를 저장한 직후 안내 링크로 넘어가 "일정 자동 만들기"를 누르면 첫 클릭이 412 충돌로 막히던 결함(더블클릭 경합이 아니라 저장 API가 공유 캐시를 갱신하지 않아 생기던 별개 문제) — `container-placement-api`·`garden-layout-api`에 캐시 무효화 추가
- 화분·베란다 시즌은 작물을 이미 배치해도 "다음 추천 단계"와 대시보드 "작물 배치" 수치가 계속 "배치 안 함"으로 남아 있던 결함
- 약 850~1300px 폭(작은 노트북 화면)에서 헤더의 "공간 추가" 버튼·계정 메뉴 글자가 줄바꿈되던 문제
- `/start` 결과 화면의 흙·모종 아이콘 — 처음에는 "실제 PNG 자산이라 이모지가 아니다"로 오탐 처리했으나, 다시 확인해 보니 Unicode 이모지와 스타일이 사실상 동일한 광택 스티커형 일러스트였다. 작물과 무관한 이 2개 아이콘만 crop-catalog의 "색상 원 + 한글 이니셜" 대체 패턴과 동일한 텍스트 배지로 교체했다(같은 화면의 6종 작물별 crop-mark 스티커는 진단 기능 전체의 시각 자산 체계라 그대로 둠).

**의도적으로 보류한 것:** 결제(구독)를 실제로 쓸 사용자가 있을지 불확실한 상태에서 광고 삽입이나 새 유료 기능 게이팅을 만드는 건 사용자 본인이 "실제 사용자가 생기고 나서 다시 논의" 하기로 결정해 진행하지 않습니다.

**로컬에 남은 미완성 변경은 없습니다** — 위 항목 전부 `develop`에 병합·푸시됐고 학교 서버(`wa26b02`)에도 배포·마이그레이션·스모크 테스트를 마쳤습니다. 닷홈(운영 백엔드)은 위 "1. 한눈에 보는 현재 상태"에 적었듯 사용자 지시로 이 단계에서는 확인·반영 대상이 아닙니다.

## 6. 디자인 작업 순서 관리

현재 순서와 상태는 [작업 목록](TASKS.md)만 갱신합니다. 완료된 항목을 이 문서의 “남은 작업”으로 중복 유지하지 않습니다.

작은 UI 작업을 DB나 API 변경으로 확대하지 않습니다. 화면에서 필요한 데이터가 이미 있으면 프론트 구조와 CSS만 수정합니다. 실제 서버 기능이 없는 알림·결제·사진 업로드를 디자인만으로 동작하는 기능처럼 표시하지 않습니다.

## 7. 디자인 방향

- 요약 화면: 사진 또는 짙은 숲색 현황 패널을 상단에 두고 핵심 수치를 오른쪽에 배치합니다.
- 관리 화면: 따뜻한 흰색 배경, 얇은 경계, 에메랄드 CTA, 20~24px 반경의 카드로 구성합니다.
- 데스크톱: 정보와 작업을 2~3열로 나누되 제목이나 설명을 덮는 절대 배치를 사용하지 않습니다.
- 모바일: 모든 최소 너비를 해제하고 핵심 입력·작업을 먼저 보여 준 뒤 요약·보조 정보를 아래에 둡니다.
- 빈 상태: 오류처럼 빨간 문구만 보여 주지 않고, 사용자가 무엇을 먼저 해야 하는지와 이동 버튼을 제공합니다.
- 작물 이미지: `public/figma`의 기존 사진을 우선 사용합니다. 없는 작물은 카테고리 색과 한글 첫 글자로 대체하며 임의의 외부 이미지를 추가하지 않습니다.
- 이모지 아이콘과 임의 SVG path를 추가하지 않습니다.
- 화분·베란다에는 격자 UI를 적용하지 않습니다.
- 원본 디자인의 영문 회사 정보·가짜 SNS·가짜 기능은 그대로 복제하지 않고 실제 심어봄 정보와 기능으로 치환합니다.

## 8. 코드 작업 원칙

- 새 유틸·타입·도우미를 만들기 전에 동일 역할이 있는지 `rg`로 검색합니다.
- 도메인 규칙은 `features/<domain>/domain`, API 통신은 `infrastructure`, 서버 상태는 `hooks`, 화면은 `components` 경계를 유지합니다.
- 오류를 빈 배열이나 성공 상태로 숨기지 않습니다.
- `as any`, `as unknown`, 빈 `catch`, 내부 구현을 과도하게 mock한 테스트를 추가하지 않습니다.
- 복잡도와 중첩을 줄이기 위해 guard clause와 작은 렌더링 컴포넌트를 사용합니다.
- 임시 미리보기 라우트와 로컬 스크린샷은 커밋하지 않습니다.
- 사용자 기능 동작을 바꾸지 않은 디자인 작업은 기존 행동 테스트를 유지하고 전체 회귀 검증으로 확인합니다.

## 9. 전체 검증 명령

```powershell
cd backend
php artisan test
vendor\bin\pint --test

cd ..\frontend
npm.cmd test
npm.cmd run typecheck
npm.cmd run lint
npm.cmd run build

cd ..
git diff --check
git status --short --branch
```

현재 Next.js 빌드에는 `metadataBase`가 없다는 비차단 경고가 있습니다. 빌드 실패는 아니며 별도 메타데이터 작업에서 해결합니다.

## 10. 배포와 운영 주의

- `develop` push 후 Vercel 프론트가 자동 배포됩니다.
- 닷홈 백엔드는 자동 배포가 아닙니다.
- 백엔드 변경이 없으면 FileZilla 업로드나 운영 마이그레이션을 하지 않습니다.
- 백엔드 변경이 있으면 로컬 테스트 후 변경 파일만 업로드하고, 필요한 경우 일회용 실행 파일로 `config:clear`와 `migrate --force`를 실행한 뒤 즉시 삭제합니다.
- 운영 수정 전에 DB 백업과 대상 파일 목록을 확인합니다.
- 운영 `.env`, `public/index.php` 경로 구성과 비밀키를 Git 파일로 덮어쓰지 않습니다.
- 카카오 로그인 운영 반영 파일과 환경 변수·Kakao Developers 설정 순서는 [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md)의 “카카오 로그인 변경 파일 수동 업로드”를 그대로 따릅니다.

## 11. 다음 AI가 바로 실행할 순서

1. `docs/README.md`, `docs/TASKS.md`, 이 문서, `frontend/AGENTS.md`, `frontend/DESIGN.md`를 끝까지 읽습니다.
2. `git status --short --branch`, `git log -5 --oneline`, `git stash list`를 확인합니다.
3. 닷홈(실제 운영 백엔드) 반영 여부는 사용자 지시로 이 단계에서는 확인하지 않습니다. 실사용 흐름 확인·배포는 학교 테스트 서버(`wa26b02.yjjob.kr`)만 사용합니다. 사용자가 먼저 요청하면 그때 [닷홈 배포 가이드](DOTHOME_DEPLOYMENT.md)를 참고합니다.
4. **바로 이어서 할 작업(권장)**: 화분 배치 그리드 캔버스의 후속 두 항목 — (a) 그룹 상자(드래그로 여러 칸 묶기, 비영구 화면 상태로 구현하기로 합의됨) (b) 모바일 바텀시트 레이아웃(사용자가 발표 때 보여준 스크린샷 기준, `reference_screenshot_folder` 로컬 메모리 확인). 시작 전에 `container-placement-grid.ts`·`container-grid-canvas.tsx`·팀원 프로토타입(UNC 경로, 위 "5. 현재 진행 상태" 참고)을 다시 훑어본다. 이 두 항목이 아니라면 [작업 목록](TASKS.md)의 첫 번째 `[ ]` 항목을 선택하고 기존 구현·테스트·디자인 공통 컴포넌트를 먼저 확인합니다. 대기 중인 항목이 없으면 사용자에게 다음 우선순위를 묻습니다.
5. `develop`에서 해당 작업만을 위한 새 `feature/*` 브랜치를 만들고, 시작 즉시 작업 목록을 `[-]`로 갱신합니다.
6. 기능 계약을 유지하면서 데스크톱 1440px과 모바일 390px을 실제 브라우저로 확인합니다. 실사용 흐름 확인이 필요하면 학교 테스트 서버(`wa26b02.yjjob.kr`, 접속 정보는 로컬 Claude Code 메모리 참고)를 활용할 수 있습니다. 임시 QA 파일과 라우트는 커밋 전에 삭제합니다.
7. 전체 검증 후 완료 항목을 즉시 `[x]`로 옮기거나 제거하고, 한글 기능·문서·병합 커밋으로 `develop`에 반영합니다.
8. 병합된 기능 브랜치는 삭제하되 `feature/yj-work`는 건드리지 않습니다.
